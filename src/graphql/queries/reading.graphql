query AllReadings {
  allReadings {
    ...StandardReading
  }
}

query CurrentlyReading {
  currentlyReading {
    ...StandardReading
  }
}

query Reading($id: Int!) {
  reading(id: $id) {
    ...StandardReading
  }
}
